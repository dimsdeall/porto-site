image: node:latest

.deploy: &deploy
  - npm install -g firebase-tools
  - pwd
  - echo "$HOME"
  - ls -a
  - cp $FIREBASE_SERVICE_ACCOUNT ./service-account.json
  - export GOOGLE_APPLICATION_CREDENTIALS="$PWD/service-account.json"
  - yarn
  - yarn build

stages:
  - deploy

production:
  stage: deploy
  before_script:
    - *deploy
  only:
    - main
  # when: manual
  allow_failure: false
  script:
    - firebase deploy -m "Pipe $CI_PIPELINE_ID Build $CI_BUILD_ID" --only hosting:$FIREBASE_TARGET_HOSTING
